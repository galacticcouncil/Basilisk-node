window.SIDEBAR_ITEMS = {"enum":[["TransportTimeoutError","Error that can be produced by the `TransportTimeout` layer."]],"struct":[["Timeout","Wraps around a `Future`. Turns the error type from `TimeoutError<Err>` to `TransportTimeoutError<Err>`."],["TransportTimeout","A `TransportTimeout` is a `Transport` that wraps another `Transport` and adds timeouts to all inbound and outbound connection attempts."]]};